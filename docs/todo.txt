
GUI: 
- выделение и копирование в буфер сообщений чата
- интерактивный редактор свойств сервиса, с отправкой запросов на сервер
- генератор текстовых фраз для тестирования сообщений
- фрейм для таблицы маршрутизации и карты сети (в виде TTreeView)

DNMP protocol:
--- линковка узлов

CHAT:
? сохранение и чтение истории сообщений

GRPC:
? обновление списков пользователей клиентов, если кто-то JOIN
? обновление текста чата при отправке
+ фрейм для GRPC, с редактором свойств

-------------------------------------------------------------------

+ самоочистка таблицы линков от неактивных линков
+ разобраться с абонентами и их списками
+ сохранять и отображать данные SRVD, полученные с сервера
+ Файлы данных (сервисов, итд) в папку с адресом владельца
+ сохранять размер и положение окна в конфиге
